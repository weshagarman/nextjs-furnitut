fragment landingPageBlocks on ComponentChoiceComponentForLandingPageBlocks {
    storySlider {
        ...storySlider
    }
    banner {
        ...banner
    }
    featureHighlights {
        ...featureHighlights
    }
    productSlider {
        ...productSlider
    }
    pictureGrid {
        ...pictureGrid
    }
}
